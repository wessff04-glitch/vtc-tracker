(function(){
    var global = (typeof self !== 'undefined') ? self : (typeof window !== 'undefined' ? window : {});
    // Incrémente manuellement à chaque déploiement
    global.APP_VERSION = "1.0.6";
})();
